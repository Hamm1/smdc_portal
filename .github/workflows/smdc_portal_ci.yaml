name: smdc_portal_ci

on:
  push:
    branches:
      - 'main'
    tags:
      - "release-v[0-9]+.[0-9]+.[0-9]+"
      - "release-v[0-9]+.[0-9]+.[0-9]+-rc[0-9]+"

jobs:
  rust:
    name: Run Cargo Tests
    runs-on: 'ubuntu-latest'

    steps:
      - uses: actions/checkout@v4

      - name: Dagger Rust tests
        run: |
          sudo apt update && sudo apt install curl unzip wget;
          curl -L https://dl.dagger.io/dagger/install.sh | BIN_DIR=$HOME/.local/bin sh;
          export PATH="$HOME/.local/bin:$PATH";
          cd .ci
          dagger call tests --src=../

  bun:
    name: Run Playwright Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Dagger Playwright tests
        run: |
          sudo apt update && sudo apt install curl unzip wget;
          curl -L https://dl.dagger.io/dagger/install.sh | BIN_DIR=$HOME/.local/bin sh;
          export PATH="$HOME/.local/bin:$PATH";
          cd .ci
          dagger call playwright --src=../

  build:
    needs: [bun, rust]
    strategy:
      matrix:
        platform: [ubuntu-latest, windows-latest]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Rust
        if: matrix.platform == 'windows-latest'
        uses: dtolnay/rust-toolchain@stable

      - name: Package install Windows
        if: matrix.platform == 'windows-latest'
        run: bun install @tauri-apps/cli-win32-x64-msvc --force

      - name: Package install Ubuntu
        if: matrix.platform == 'ubuntu-latest'
        run: |
          bun install @tauri-apps/cli-linux-x64-gnu --force \
          && sudo apt update \
          && sudo apt install libdbus-1-dev pkg-config \
          zlib1g libevent-dev \
          build-essential curl wget libssl-dev \
          libgtk-3-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev \
          librsvg2-dev libfuse2 libappindicator3-dev libwebkit2gtk-4.1-dev \
          patchelf

      - name: Run build
        run: bun run build:bun

      - name: Upload Ubuntu binary
        if: matrix.platform == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: linux-binary
          path: ./src-tauri/target/release/smdc_portal

      - name: Upload Windows binaries
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-binaries
          path: |
            ./src-tauri/target/release/smdc_portal.exe
            ./src-tauri/target/release/bundle/msi/smdc_portal_0.1.0_x64_en-US.msi

  release:
    name: Create GitHub release
    runs-on: ubuntu-latest
    needs: build
    environment: production

    outputs:
      release_created: ${{ steps.semantic_release.outputs.release_created }}
      version: ${{ steps.semantic_release.outputs.version }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Download Linux binary
        uses: actions/download-artifact@v4
        with:
          name: linux-binary
          path: ./artifacts/

      - name: Download Windows binaries
        uses: actions/download-artifact@v4
        with:
          name: windows-binaries
          path: ./artifacts/

      - name: Make Linux binary executable
        run: chmod +x ./artifacts/smdc_portal

      - name: Create semantic-release config
        run: |
          cat > .releaserc.json << 'EOF'
          {
            "branches": ["main"],
            "plugins": [
              "@semantic-release/commit-analyzer",
              "@semantic-release/release-notes-generator",
              [
                "@semantic-release/npm",
                {
                  "npmPublish": false
                }
              ],
              [
                "@semantic-release/github",
                {
                  "assets": [
                    {
                      "path": "artifacts/smdc_portal",
                      "name": "SMDC-Portal",
                      "label": "SMDC-Portal (Linux)"
                    },
                    {
                      "path": "artifacts/smdc_portal.exe",
                      "name": "SMDC-Portal.exe",
                      "label": "SMDC-Portal (Windows)"
                    },
                    {
                      "path": "artifacts/smdc_portal_0.1.0_x64_en-US.msi",
                      "name": "SMDC-Portal.msi",
                      "label": "SMDC-Portal (Windows MSI)"
                    }
                  ]
                }
              ]
            ]
          }
          EOF
          echo '{"name": "smdc_portal", "version": "0.0.0-development", "private": true}' > package.json

      - name: Release on GitHub with assets
        id: semantic_release
        run: |
          sudo apt-get update && sudo apt-get install -y jq
          npx semantic-release 2>&1 | tee semantic-release.log
          if grep -q "There are no relevant changes, so no new version is released" semantic-release.log; then
            echo "No release created by semantic-release - exiting workflow"
            echo "release_created=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$LATEST_TAG" ]; then
            echo "No release tag found - exiting workflow"
            echo "release_created=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          if [ -f package.json ]; then
            VERSION=$(jq -r '.version' package.json)
            echo "Found version in package.json: $VERSION"
          else
            VERSION=$(echo "$LATEST_TAG" | sed 's/^v//')
            echo "Using git tag version: $VERSION"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "release_created=true" >> $GITHUB_OUTPUT
          echo "Release created successfully with version: $VERSION"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: "dummy-token"
